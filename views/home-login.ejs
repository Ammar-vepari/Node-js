<%- include('includes/header')%>  
  <div class="container py-md-5 container--narrow">
    <%-include('includes/validationResult')%>

    <%if(posts.length){%>
      <%posts.forEach(function(p){%>
        <a href="/post/<%=p._id%>" class="list-group-item list-group-item-action">
          <img class="avatar-tiny" src="<%=p.author.avatar%>">By <%=p.author.username%> on <%=p.CreatedDate.getMonth()+1 %>/<%=p.CreatedDate.getDate()%>/<%=p.CreatedDate.getFullYear()%>
          <strong><%=p.title%></strong>
        </a>

      <%})%>

    <%}else{%>

      <div class="text-center">
        <h2>Hello <strong><%=user.username%></strong>, your feed is empty.</h2>
        <p class="lead text-muted">Your feed displays the latest posts from the people you follow. If you don&rsquo;t have any friends to follow that&rsquo;s okay; you can use the &ldquo;Search&rdquo; feature in the top menu bar to find content written by people with similar interests and then follow them.</p>
      </div>  
      

    <%}%>

  </div>
    
  
  <!-- footer begins -->
  <%-include('includes/footer')%>